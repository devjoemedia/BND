import FormHandler from 'markojs-form'

static const options = {
    key: 'edit-trans',
    autosave: true,
    crosscheck: true
}

class {
  onCreate(input){
    const initialForm = { attribute: 'trans attribute', host: 'trans host', port:"trans port", security: 'trans security', appOrigin: "trans origin", userAgent: "trans userAgent", userName: "trans username", password: "trans password"}

    this.fhandler = new FormHandler( options )
    //console.log(input);
    this.fhandler.bind(this, initialForm)
  }

  handleSubmit(e) {
    e.preventDefault()
    console.log(this.state.form)
    console.log("create template")
  }
}

<Dashboard navigate=input.navigate>
  <div class="mb-3">
    <h3 class="text-center">General Email Sender</h3>
  </div>

  <section>
    <div class="row">
      <div class="col-12">
        <form class="form-validate">
          <div class="row">
            <div class="col-12 col-sm-10 col-lg-8 mx-auto">
              <div class="form-group">
                <div class="controls">
                  <label>Attribute</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="attribute"
                    value=state.form.attribute
                    name="attribute"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>Host</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="host"
                    value=state.form.host
                    name="host"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>Port</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="port"
                    value=state.form.port
                    name="port"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>Security</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="security"
                    value=state.form.security
                    name="security"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>App Origin</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="App Origin"
                    value=state.form.appOrigin
                    name="appOrigin"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>User Agent</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="User Agent"
                    value=state.form.userAgent
                    name="userAgent"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>Username</label>
                  <input
                    type="text"
                    class="form-control form-control-lg"
                    placeholder="user name"
                    value=state.form.userName
                    name="userName"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="form-group">
                <div class="controls">
                  <label>Password</label>
                  <input
                    type="password"
                    class="form-control form-control-lg"
                    placeholder="password"
                    value=state.form.password
                    name="password"
                    on-change("__onChange")
                  />
                </div>
              </div>
              <div class="my-1" >
                <button type="reset" class="btn btn-lg btn-light mr-1" on-click(()=> input.navigate('/transporters'))>Cancel</button>
                <button type="submit" class="btn btn-lg btn-primary" on-click("handleSubmit")>
                  Save
                </button>
              </div>
            </div>
          </div>
        </form>
      </div>
    </div>
</section>
</Dashboard>